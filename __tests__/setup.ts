import {NativeModules} from "react-native";

// jest.mock('react-redux', () => ({
//     ...(jest.requireActual('react-redux') as object),
//     useSelector: jest.fn(),
//     useDispatch: jest.fn(),
// }))
